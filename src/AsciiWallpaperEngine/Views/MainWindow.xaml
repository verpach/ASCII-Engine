<Window x:Class="AsciiWallpaperEngine.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AsciiWallpaperEngine.ViewModels"
        mc:Ignorable="d"
        Title="ASCII Wallpaper Engine"
        Width="980"
        Height="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <TabControl>
            <TabItem Header="Главная">
                <StackPanel Margin="8" DataContext="{Binding}">
                    <CheckBox Content="Включить ASCII wallpaper" IsChecked="{Binding IsWallpaperEnabled}"/>
                    <Button Margin="0,8,0,0" Content="Применить" Command="{Binding ApplyProfileCommand}" Width="140"/>
                    <TextBlock Margin="0,10,0,0" Text="Профиль" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding Themes.Themes}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" Width="280"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Темы" DataContext="{Binding Themes}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Импорт" Command="{Binding ImportThemeCommand}" Width="120"/>
                        <Button Margin="8,0,0,0" Content="Предпросмотр" Command="{Binding PreviewCommand}" Width="120"/>
                    </StackPanel>
                    <Grid Grid.Row="1" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ListBox ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" DisplayMemberPath="Name"/>
                        <TextBox Grid.Column="1" Margin="8,0,0,0" Text="{Binding PreviewText}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" FontFamily="Consolas"/>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Видео" DataContext="{Binding Video}">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="500" Text="{Binding VideoPath}"/>
                        <Button Margin="8,0,0,0" Content="Выбрать" Command="{Binding BrowseVideoCommand}" Width="120"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock VerticalAlignment="Center" Text="Качество:"/>
                        <ComboBox Margin="8,0,0,0" Width="160" SelectedItem="{Binding VideoQuality}">
                            <ComboBoxItem Content="Низкое"/>
                            <ComboBoxItem Content="Среднее"/>
                            <ComboBoxItem Content="Высокое"/>
                        </ComboBox>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center" Text="FPS:"/>
                        <TextBox Margin="8,0,0,0" Width="80" Text="{Binding VideoFps}"/>
                    </StackPanel>
                    <Button Margin="0,10,0,0" Content="Применить" Command="{Binding ApplyVideoSettingsCommand}" Width="140"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Производительность" DataContext="{Binding Performance}">
                <StackPanel Margin="8">
                    <TextBlock Text="Ограничение FPS"/>
                    <Slider Minimum="15" Maximum="120" Value="{Binding TargetFps}" Width="300"/>
                    <TextBlock Margin="0,6,0,0" Text="{Binding TargetFps, StringFormat='Текущее значение: {0}'}"/>
                    <CheckBox Margin="0,10,0,0" Content="Пауза при полноэкранных приложениях" IsChecked="{Binding PauseFullscreen}"/>
                    <CheckBox Margin="0,6,0,0" Content="Пауза при работе от батареи" IsChecked="{Binding PauseOnBattery}"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Настройки" DataContext="{Binding Settings}">
                <StackPanel Margin="8">
                    <CheckBox Content="Автозапуск" IsChecked="{Binding AutoStart}"/>
                    <CheckBox Margin="0,6,0,0" Content="Запуск в трей" IsChecked="{Binding StartMinimizedToTray}"/>
                    <TextBlock Margin="0,10,0,0" Text="Горячая клавиша переключения темы"/>
                    <TextBox Width="180" Text="{Binding HotkeyNextTheme}"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
